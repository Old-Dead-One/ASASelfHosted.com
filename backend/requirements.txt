# Core framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Supabase client
supabase==2.8.0

# Stripe
stripe==11.0.0

# Monitoring (optional - app works without it)
# sentry-sdk[fastapi]==2.19.0

# HTTP client for external requests
# Note: Supabase 2.8.0 requires httpx<0.28 and >=0.24
httpx>=0.24,<0.28

# Environment and security
python-dotenv==1.0.1
PyJWT==2.9.0  # Note: Import as 'jwt' in code, not 'PyJWT'
cryptography==43.0.1  # For stronger signing/key handling (agent keys, etc.)

# Testing
pytest==8.3.3
pytest-asyncio==0.24.0
# pytest-httpx removed due to dependency conflict with supabase
# pytest-httpx requires httpx==0.28.* but supabase requires httpx<0.28
# Use httpx directly or mock HTTP requests for testing instead

# Logging (optional - use Python's logging or structlog)
# structlog==24.1.0  # Structured logging (uncomment if needed)

# File uploads (add when needed)
# python-multipart==0.0.9  # Required for FastAPI form uploads