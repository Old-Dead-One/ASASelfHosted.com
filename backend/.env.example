# ASASelfHosted.com - Environment Variables Example
# Copy this file to .env and fill in your values
# Never commit .env to version control (it's in .gitignore)

# Environment
# Options: local, development, staging, production
ENV=local
DEBUG=false

# API
API_V1_PREFIX=/api/v1

# CORS (comma-separated list of allowed origins)
# For local development, include both frontend ports
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Supabase Configuration
# Get these from your Supabase project: Settings → API
# Project URL: https://<project-ref>.supabase.co
SUPABASE_URL=https://xxxxx.supabase.co
# anon public key (safe to expose, used in frontend)
SUPABASE_ANON_KEY=your_anon_key_here
# service_role key (KEEP SECRET - backend only, bypasses RLS)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# JWT Verification (JWKS-based, not secret-based)
# Get these from your Supabase project: Settings → API → JWT Settings
SUPABASE_JWT_ISSUER=https://xxxxx.supabase.co/auth/v1
SUPABASE_JWKS_URL=https://xxxxx.supabase.co/auth/v1/.well-known/jwks.json
SUPABASE_JWT_AUDIENCE=authenticated

# Auth Bypass (local development only)
# DO NOT enable in staging/production - will raise ValueError
AUTH_BYPASS_LOCAL=false

# Admin: comma-separated user UUIDs allowed to access /api/v1/admin/* endpoints
# ADMIN_USER_IDS=uuid1,uuid2

# Agent version enforcement (optional): reject heartbeats from agents below this version
# MIN_AGENT_VERSION=0.1.0

# Directory View Name (default: directory_view)
# Configurable for future migrations
DIRECTORY_VIEW_NAME=directory_view

# Stripe (optional for development)
# Only needed if implementing subscription features
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Sentry (optional)
# Error tracking and monitoring
SENTRY_DSN=

# Per-user limits (abuse prevention)
# MAX_SERVERS_PER_USER: 1–100; default 14 (aligns with official + DLC maps)
# MAX_SERVERS_PER_USER=14

# Rate Limiting
# Enable rate limiting for production
RATE_LIMIT_ENABLED=false

# CurseForge API (for mod name resolution)
# Get API key from: https://console.curseforge.com/
CURSEFORGE_API_KEY=your_curseforge_api_key_here
# Optional: Pre-discovered ASA game ID (will auto-discover if not set)
# CURSEFORGE_ASA_GAME_ID=
